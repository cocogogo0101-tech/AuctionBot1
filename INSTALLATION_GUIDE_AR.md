# 📘 دليل التثبيت والتشغيل - AuctionBot v2.0

## 🎯 نظرة عامة

هذا الدليل الشامل سيساعدك على تثبيت وتشغيل بوت المزادات بنجاح.

---

## ✅ المتطلبات الأساسية

### 1. Python
- الإصدار المطلوب: **Python 3.10 أو أحدث**
- تحقق من الإصدار:
```bash
python --version
```

### 2. Discord Bot Token
- اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
- أنشئ تطبيق جديد
- انتقل إلى قسم "Bot"
- اضغط "Reset Token" وانسخ التوكن
- **مهم جداً**: لا تشارك التوكن مع أحد!

### 3. صلاحيات البوت
عند دعوة البوت، تأكد من منحه الصلاحيات التالية:
- ✅ Send Messages
- ✅ Embed Links
- ✅ Read Message History
- ✅ Add Reactions
- ✅ Manage Messages
- ✅ View Channel

---

## 📦 التثبيت خطوة بخطوة

### الخطوة 1: تحميل الملفات
1. حمّل جميع ملفات البوت
2. فك الضغط في مجلد جديد

### الخطوة 2: تثبيت المكتبات
افتح Terminal/Command Prompt في مجلد البوت واكتب:

```bash
pip install -r requirements.txt
```

أو إذا كنت تستخدم Python 3:
```bash
pip3 install -r requirements.txt
```

### الخطوة 3: إنشاء ملف الإعدادات
1. انسخ ملف `.env.example` وسمّه `.env`
2. افتح `.env` بمحرر نصوص
3. أضف توكن البوت:

```env
BOT_TOKEN=ضع_توكن_البوت_هنا
```

**مثال:**
```env
BOT_TOKEN=MTIzNDU2Nzg5MDEyMzQ1Njc4OQ.GhI7jK.L8mNoPqRsTuVwXyZaBcDeFgHiJkLmNoPqR
```

### الخطوة 4: تشغيل البوت
```bash
python bot.py
```

إذا نجح التشغيل، سترى:
```
Logged in as: YourBotName#1234
✓ Database initialized successfully
✓ Commands synced to guild XXXXX
Bot is ready and operational!
```

---

## ⚙️ الإعداد الأولي في Discord

بعد تشغيل البوت، استخدم الأوامر التالية **بالترتيب**:

### 1️⃣ تحديد السيرفر المسموح
```
/config_set_server
```
هذا الأمر يحصر البوت في سيرفرك فقط.

### 2️⃣ تحديد رتبة الرواد
```
/config_set_role role:@رواد_المزاد
```
الأعضاء الذين يملكون هذه الرتبة يمكنهم المزايدة.

### 3️⃣ تحديد القنوات
```
/config_set_channels auction_channel:#مزادات log_channel:#سجلات
```
- `auction_channel`: القناة التي ستظهر فيها لوحة المزاد
- `log_channel`: القناة التي ستُسجل فيها تفاصيل المزادات

### 4️⃣ تحديد العمولة والعملة
```
/config_set_misc commission:20 currency:كريديت
```
- `commission`: نسبة العمولة (مثلاً 20 يعني 20%)
- `currency`: اسم العملة المستخدمة

### 5️⃣ (اختياري) تحديد رمز سري
```
/config_set_secret secret:كلمة_سرية_قوية
```
هذا الرمز يُستخدم للإجراءات الحساسة.

### ✅ التحقق من الإعدادات
```
/config_show
```
سيعرض لك جميع الإعدادات الحالية.

---

## 🎯 كيفية فتح مزاد

بعد إتمام الإعداد، يمكنك فتح أول مزاد:

```
/auction_open start_bid:250k min_increment:50k duration_minutes:5
```

**شرح المعاملات:**
- `start_bid`: سعر البداية (يدعم: 250k, 2.5m, 1000000)
- `min_increment`: أقل زيادة مسموحة
- `duration_minutes`: مدة المزاد بالدقائق (1-1440)

**أمثلة:**
```
/auction_open start_bid:500k min_increment:100k duration_minutes:10
/auction_open start_bid:1m min_increment:250k duration_minutes:30
/auction_open start_bid:5m min_increment:500k duration_minutes:60
```

---

## 💰 كيفية المزايدة

عندما يفتح المزاد، ستظهر لوحة تحتوي على:
- معلومات المزاد
- أعلى مزايدة حالية
- أزرار المزايدة

**الأزرار المتاحة:**
1. **+1K**: يزيد 1,000
2. **+100K**: يزيد 100,000
3. **+500K**: يزيد 500,000
4. **Custom**: أدخل مبلغ مخصص

**ملاحظات:**
- ⏰ يوجد فترة انتظار 2 ثانية بين كل مزايدة
- 👑 لا يمكنك المزايدة إذا كنت أعلى مزايد حالياً
- ✅ يجب أن تزيد على الأقل بمقدار Min Increment

---

## 🛠️ إدارة المزادات

### إنهاء المزاد يدوياً
```
/auction_end
```
يُنهي المزاد فوراً ويُعلن الفائز.

### حذف آخر مزايدة (للأخطاء)
```
/auction_undo_last
```
يحذف آخر مزايدة (مفيد في حالة وجود خطأ).

---

## 🐛 أوامر التشخيص

### معلومات حالة البوت
```
/debug_status
```
يعرض:
- حالة البوت
- نوع قاعدة البيانات المستخدمة
- عدد السيرفرات
- المزاد النشط
- الإعدادات

### معلومات المزاد المفصّلة
```
/debug_auction
```
يعرض:
- تفاصيل المزاد
- جميع المزايدات
- الوقت المتبقي
- رسالة اللوحة

### إعادة الاتصال بقاعدة البيانات
```
/db_retry
```
يحاول الاتصال بـ PostgreSQL إذا كان البوت يستخدم SQLite.

---

## 🎨 تخصيص الإيموجي

### تعيين إيموجي مخصص
```
/emoji_set name:fire emoji:🔥
/emoji_set name:crown emoji:<:crown:123456789>
```

**الإيموجي المتاحة افتراضياً:**
- `fire` 🔥 - النار
- `money` 💰 - المال
- `trophy` 🏆 - الكأس
- `celebrate` 🎉 - الاحتفال
- `crown` 👑 - التاج
- `rocket` 🚀 - الصاروخ
- `spark` ✨ - اللمعان
- وأكثر...

### عرض جميع الإيموجي
```
/emoji_list
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة: البوت لا يرد
**الحل:**
1. تأكد أن البوت متصل (يظهر أخضر في Discord)
2. استخدم `/debug_status` للتحقق من الحالة
3. تأكد من صلاحيات البوت في القنوات
4. تحقق من Terminal للأخطاء

### المشكلة: لا أستطيع فتح مزاد
**الحل:**
1. تحقق من الإعدادات: `/config_show`
2. تأكد أنك تملك الصلاحيات المطلوبة
3. تأكد أنه لا يوجد مزاد نشط حالياً
4. تحقق من صلاحيات البوت في قناة المزاد

### المشكلة: الإيموجي لا يظهر
**الحل:**
1. تأكد أن الإيموجي من نفس السيرفر
2. استخدم صيغة `<:name:id>` للإيموجي المخصص
3. استخدم `/emoji_list` لعرض الإيموجي المكونة

### المشكلة: خطأ في قاعدة البيانات
**الحل:**
1. البوت سيستخدم SQLite تلقائياً
2. استخدم `/db_retry` لإعادة المحاولة
3. تحقق من `DATABASE_URL` في `.env`

---

## 📊 قاعدة البيانات

### SQLite (افتراضي)
- يُنشأ ملف `local_db.sqlite` تلقائياً
- لا يحتاج إعداد إضافي
- مناسب للسيرفرات الصغيرة والمتوسطة

### PostgreSQL (اختياري - للسيرفرات الكبيرة)
أضف في `.env`:
```env
DATABASE_URL=postgresql://username:password@host:port/database
```

**مثال:**
```env
DATABASE_URL=postgresql://auctionbot:mypass123@localhost:5432/auctions
```

---

## 🔒 الأمان والصلاحيات

### مستويات الوصول:

#### 1. مزايد عادي
- **المطلوب:** رتبة الرواد المحددة
- **يمكنه:** المزايدة فقط

#### 2. مدير المزادات
- **المطلوب:** رتبة الرواد + صلاحية Manage Guild
- **يمكنه:** فتح وإنهاء المزادات، حذف المزايدات

#### 3. مسؤول الإعدادات
- **المطلوب:** صلاحية Manage Server/Roles/Channels
- **يمكنه:** تغيير جميع الإعدادات

---

## 🔄 التحديث من نسخة سابقة

### خطوات الترقية:

1. **نسخ احتياطي لقاعدة البيانات**:
```bash
cp local_db.sqlite local_db.sqlite.backup
```

2. **استبدال الملفات**:
- احتفظ بملف `.env` الحالي
- استبدل باقي الملفات بالنسخة الجديدة

3. **تحديث المكتبات**:
```bash
pip install -r requirements.txt --upgrade
```

4. **إعادة التشغيل**:
```bash
python bot.py
```

5. **التحقق**:
```
/debug_status
```

---

## 💡 نصائح للاستخدام الأمثل

### للإداريين:
1. ✅ اضبط `duration_minutes` حسب نوع المزاد
2. ✅ راجع `/config_show` بانتظام
3. ✅ فعّل `DEBUG_MODE` عند المشاكل
4. ✅ استخدم `/debug_auction` لمتابعة المزادات
5. ✅ احتفظ بنسخة احتياطية من قاعدة البيانات

### للأعضاء:
1. 👀 راقب الوقت المتبقي في اللوحة
2. ⚡ استخدم الأزرار السريعة للسرعة
3. 📝 استخدم Custom للمبالغ الدقيقة
4. 🎯 انتبه لرسالة العدّ التنازلي

---

## 📞 الدعم والمساعدة

### إذا واجهت مشكلة:

1. **التحقق من اللوق**:
```bash
# في Terminal
# ستظهر الأخطاء مباشرة
```

2. **تفعيل Debug Mode**:
```env
DEBUG_MODE=True
```

3. **استخدام أوامر التشخيص**:
```
/debug_status
/debug_auction
```

4. **البحث عن حلول**:
- راجع `CHANGELOG.md`
- راجع `README.md`
- ابحث في Issues على GitHub

---

## 🎉 مبروك!

إذا وصلت هنا، فأنت جاهز لبدء استخدام بوت المزادات! 🚀

**خطوات سريعة للبدء:**
1. ✅ شغّل البوت: `python bot.py`
2. ✅ اضبط الإعدادات: `/config_set_*`
3. ✅ افتح أول مزاد: `/auction_open`
4. ✅ استمتع! 🎯

---

## 📚 موارد إضافية

- 📖 [README.md](README.md) - الدليل الكامل
- 📝 [CHANGELOG.md](CHANGELOG.md) - سجل التغييرات
- 🔧 [.env.example](.env.example) - مثال الإعدادات

---

**صُنع بحب ❤️ لمجتمع ديسكورد العربي**

**آخر تحديث: 2024**
**الإصدار: 2.0.0 Enhanced**
